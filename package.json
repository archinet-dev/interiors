{
  "name": "space-makeover-visualizer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bun server/proxy for the Space Makeover Visualizer. The UI has no build step and no dependencies; the backend runs under Bun (H6).",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "start": "bun server/index.js",
    "dev": "bun --watch server/index.js"
  }
}
